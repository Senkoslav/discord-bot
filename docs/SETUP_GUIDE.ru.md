# 🚀 Пошаговая инструкция по настройке

## Шаг 1: Создание бота в Discord

### 1.1 Создай приложение
1. Открой https://discord.com/developers/applications
2. Нажми **"New Application"** (синяя кнопка справа сверху)
3. Придумай имя для бота (например, "Music Bot")
4. Нажми **"Create"**

### 1.2 Настрой бота
1. В левом меню выбери **"Bot"**
2. Нажми **"Reset Token"** → **"Yes, do it!"**
3. **ВАЖНО**: Скопируй токен и сохрани его (он больше не покажется!)
   - Это будет твой `DISCORD_TOKEN`
4. Прокрути вниз до раздела **"Privileged Gateway Intents"**
5. Включи следующие опции:
   - ✅ **Presence Intent** (опционально)
   - ✅ **Server Members Intent** (опционально)
   - ✅ **Message Content Intent** (ОБЯЗАТЕЛЬНО!)
6. Нажми **"Save Changes"**

### 1.3 Настрой права бота
1. В левом меню выбери **"OAuth2"** → **"URL Generator"**
2. В разделе **"Scopes"** выбери:
   - ✅ `bot`
   - ✅ `applications.commands`
3. В разделе **"Bot Permissions"** выбери:
   
   **Text Permissions (Текстовые права):**
   - ✅ Send Messages (Отправка сообщений)
   - ✅ Embed Links (Встраивание ссылок)
   - ✅ Read Message History (Чтение истории сообщений)
   
   **Voice Permissions (Голосовые права):**
   - ✅ Connect (Подключение)
   - ✅ Speak (Воспроизведение звука)
   - ✅ Use Voice Activity (Голосовая активность)

4. Внизу страницы скопируй **"Generated URL"**
5. Открой эту ссылку в браузере
6. Выбери свой сервер и нажми **"Авторизовать"**

## Шаг 2: Получи свой Discord ID

### 2.1 Включи режим разработчика
1. Открой Discord
2. Нажми на шестерёнку (Настройки пользователя)
3. Перейди в **"Расширенные"** (Advanced)
4. Включи **"Режим разработчика"** (Developer Mode)

### 2.2 Скопируй свой ID
1. Нажми правой кнопкой мыши на своё имя/аватар
2. Выбери **"Копировать ID пользователя"** (Copy User ID)
3. Это будет твой `BOT_OWNER_ID` (например: 123456789012345678)

## Шаг 3: Настройка файла .env

### 3.1 Создай файл конфигурации
```bash
cd music-bot
cp .env.example .env
```

### 3.2 Открой .env в текстовом редакторе
Например:
- Windows: `notepad .env`
- Linux/Mac: `nano .env` или `vim .env`

### 3.3 Заполни обязательные поля
```env
# Вставь токен бота (из шага 1.2)
DISCORD_TOKEN=твой_длинный_токен_здесь

# Вставь свой Discord ID (из шага 2.2)
BOT_OWNER_ID=123456789012345678

# Оставь SQLite (проще для начала)
USE_REDIS=false
```

### 3.4 Сохрани файл
- Notepad: Файл → Сохранить
- Nano: Ctrl+O → Enter → Ctrl+X
- Vim: Esc → `:wq` → Enter

## Шаг 4: Запуск бота

### 4.1 Убедись, что Docker установлен
```bash
docker --version
docker compose version
```

Если не установлен:
- Windows/Mac: Скачай [Docker Desktop](https://www.docker.com/products/docker-desktop)
- Linux: `sudo apt install docker.io docker-compose-plugin`

### 4.2 Запусти бота
```bash
cd music-bot
docker compose up --build
```

При первом запуске:
- Docker скачает образ Python (~200 МБ)
- Установит все зависимости
- Соберёт контейнер
- Это займёт 3-5 минут

### 4.3 Проверь, что бот запустился
В логах должно быть:
```
Bot is ready! Logged in as YourBotName (ID: ...)
Connected to X guilds
```

## Шаг 5: Проверка работы

### 5.1 Проверь, что бот онлайн
В Discord бот должен быть в списке участников с зелёным статусом

### 5.2 Попробуй команды
1. Зайди в текстовый канал
2. Напиши `/ping` — бот должен ответить
3. Напиши `/help` — увидишь список команд

### 5.3 Проверь музыку
1. Зайди в голосовой канал
2. Напиши `/play never gonna give you up`
3. Бот должен подключиться и начать играть музыку

## 🎉 Готово!

Бот работает! Теперь можешь:
- Использовать `/help` для списка команд
- Создать роль "DJ" на сервере для доверенных пользователей
- Настроить дополнительные параметры в `.env`

## 🛠️ Дополнительные настройки

### Изменить громкость по умолчанию
В `.env`:
```env
DEFAULT_VOLUME=80  # 80% вместо 100%
```

### Изменить таймаут отключения
В `.env`:
```env
INACTIVITY_TIMEOUT=600  # 10 минут вместо 5
```

### Включить подробные логи
В `.env`:
```env
LOG_LEVEL=DEBUG
```

После изменения `.env`:
```bash
docker compose down
docker compose up --build
```

## ❓ Частые проблемы

### Бот не запускается
**Ошибка**: `DISCORD_TOKEN environment variable is required`
- **Решение**: Проверь, что в `.env` есть токен и нет лишних пробелов

**Ошибка**: `Improper token has been passed`
- **Решение**: Токен неправильный. Сбрось токен в Discord Developer Portal и скопируй новый

### Бот не отвечает на команды
**Проблема**: Пишу `/play`, но ничего не происходит
- **Решение 1**: Подожди 1-2 минуты после запуска (команды синхронизируются)
- **Решение 2**: Используй команду `/sync` (только для владельца)
- **Решение 3**: Перезапусти бота: `docker compose restart`

### Бот не воспроизводит музыку
**Проблема**: Бот подключается, но нет звука
- **Решение 1**: Проверь права бота (Connect + Speak)
- **Решение 2**: Попробуй другое видео
- **Решение 3**: Посмотри логи: `docker compose logs bot`

### Как остановить бота
```bash
# В терминале где запущен бот:
Ctrl+C

# Или:
docker compose down
```

### Как запустить в фоне
```bash
docker compose up -d
```

Посмотреть логи:
```bash
docker compose logs -f bot
```

## 📞 Нужна помощь?

1. Проверь логи: `docker compose logs bot`
2. Убедись, что все шаги выполнены правильно
3. Проверь, что `.env` файл заполнен корректно
4. Убедись, что Docker запущен

---

**Удачи! 🎵**
