# ❓ Часто задаваемые вопросы (FAQ)

## 🔑 Получение токенов и ID

### Где найти DISCORD_TOKEN?

1. **Открой**: https://discord.com/developers/applications
2. **Создай приложение**: Нажми "New Application"
3. **Перейди в Bot**: Левое меню → "Bot"
4. **Получи токен**: Нажми "Reset Token" → "Yes, do it!"
5. **Скопируй**: Нажми "Copy" и сохрани токен

**Важно**: 
- Токен показывается только один раз!
- Если потерял — нажми "Reset Token" снова
- Никому не показывай токен (это как пароль)

**Пример токена**:
```
MTIzNDU2Nzg5MDEyMzQ1Njc4.GhJKlM.nOpQrStUvWxYz1234567890AbCdEfGhIjKl
```

---

### Где найти BOT_OWNER_ID?

1. **Включи режим разработчика** в Discord:
   - Настройки → Расширенные → "Режим разработчика" ВКЛ

2. **Получи свой ID**:
   - Правой кнопкой на своё имя
   - "Копировать ID пользователя"

**Пример ID**:
```
123456789012345678
```

Это просто число из 18 цифр.

---

### Что такое Redis и нужен ли он мне?

**Redis** — это быстрая база данных в памяти.

**Нужен ли тебе Redis?**

❌ **НЕТ, если**:
- У тебя один бот на 1-10 серверах
- Ты только начинаешь
- Хочешь простую настройку

✅ **ДА, если**:
- У тебя бот на 50+ серверах
- Запускаешь несколько копий бота
- Нужна максимальная скорость

**Для начала используй SQLite** (по умолчанию):
```env
USE_REDIS=false
```

**Если решишь использовать Redis**:
```env
USE_REDIS=true
REDIS_URL=redis://redis:6379/0
```

И запусти:
```bash
docker compose --profile redis up --build
```

---

### Что такое REDIS_URL?

Это адрес для подключения к Redis.

**Формат**:
```
redis://[пользователь]:[пароль]@хост:порт/база
```

**Для Docker** (по умолчанию):
```env
REDIS_URL=redis://redis:6379/0
```

Где:
- `redis` — имя контейнера (Docker сам найдёт)
- `6379` — стандартный порт Redis
- `0` — номер базы данных (можно от 0 до 15)

**Не меняй это**, если используешь Docker Compose из проекта!

---

## ⚙️ Какие опции включить в .env?

### Минимальная конфигурация (ОБЯЗАТЕЛЬНО)

```env
# Токен бота (получи в Discord Developer Portal)
DISCORD_TOKEN=твой_токен_здесь

# Твой Discord ID (для owner-команд)
BOT_OWNER_ID=твой_id_здесь

# Использовать SQLite (проще для начала)
USE_REDIS=false
```

**Этого достаточно для запуска!**

---

### Рекомендуемая конфигурация

```env
# ============ ОБЯЗАТЕЛЬНО ============
DISCORD_TOKEN=твой_токен_здесь
BOT_OWNER_ID=твой_id_здесь

# ============ БАЗА ДАННЫХ ============
USE_REDIS=false
# REDIS_URL=redis://redis:6379/0  # Раскомментируй если USE_REDIS=true

# ============ НАСТРОЙКИ БОТА ============
# Префикс для текстовых команд (в основном используются slash-команды)
COMMAND_PREFIX=!

# Громкость по умолчанию (0-200, где 100 = нормально)
DEFAULT_VOLUME=100

# Максимум треков в очереди на сервер
MAX_QUEUE_SIZE=500

# Отключение через N секунд бездействия
INACTIVITY_TIMEOUT=300

# Максимум команд в минуту на пользователя
RATE_LIMIT_COMMANDS=20

# Уровень логирования (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
```

---

### Продвинутая конфигурация

```env
# Всё из рекомендуемой конфигурации +

# Файл cookies для YouTube (для видео 18+)
YOUTUBE_COOKIES_PATH=/app/cookies.txt
```

---

## 🎛️ Объяснение каждой опции

### DISCORD_TOKEN
- **Что**: Токен для авторизации бота
- **Обязательно**: ✅ ДА
- **Где взять**: Discord Developer Portal → Bot → Reset Token
- **Пример**: `MTIzNDU2...`

### BOT_OWNER_ID
- **Что**: Твой Discord ID для owner-команд
- **Обязательно**: ⚠️ Желательно (для `/shutdown`, `/reload`)
- **Где взять**: Правой кнопкой на себя → Копировать ID
- **Пример**: `123456789012345678`

### USE_REDIS
- **Что**: Использовать Redis вместо SQLite
- **Обязательно**: ❌ НЕТ
- **Значения**: `true` или `false`
- **По умолчанию**: `false`
- **Когда менять**: Если нужна высокая производительность

### REDIS_URL
- **Что**: Адрес Redis сервера
- **Обязательно**: ❌ НЕТ (только если USE_REDIS=true)
- **По умолчанию**: `redis://redis:6379/0`
- **Когда менять**: Если используешь внешний Redis

### COMMAND_PREFIX
- **Что**: Префикс для текстовых команд
- **Обязательно**: ❌ НЕТ
- **По умолчанию**: `!`
- **Пример**: `!play`, `!skip`
- **Примечание**: Slash-команды (`/play`) работают всегда

### DEFAULT_VOLUME
- **Что**: Громкость при запуске (0-200)
- **Обязательно**: ❌ НЕТ
- **По умолчанию**: `100`
- **Рекомендуется**: 
  - `80` — для фоновой музыки
  - `100` — нормальная громкость
  - `120-150` — для вечеринок

### MAX_QUEUE_SIZE
- **Что**: Максимум треков в очереди
- **Обязательно**: ❌ НЕТ
- **По умолчанию**: `500`
- **Рекомендуется**:
  - `100` — для маленьких серверов
  - `500` — для средних
  - `1000+` — для больших

### INACTIVITY_TIMEOUT
- **Что**: Через сколько секунд отключиться при бездействии
- **Обязательно**: ❌ НЕТ
- **По умолчанию**: `300` (5 минут)
- **Рекомендуется**:
  - `180` (3 мин) — для экономии ресурсов
  - `600` (10 мин) — для удобства
  - `1800` (30 мин) — для долгих сессий

### RATE_LIMIT_COMMANDS
- **Что**: Максимум команд в минуту на пользователя
- **Обязательно**: ❌ НЕТ
- **По умолчанию**: `20`
- **Рекомендуется**:
  - `10` — строгое ограничение
  - `20` — нормальное
  - `50` — мягкое

### LOG_LEVEL
- **Что**: Сколько информации выводить в логи
- **Обязательно**: ❌ НЕТ
- **По умолчанию**: `INFO`
- **Значения**:
  - `DEBUG` — всё подряд (для отладки)
  - `INFO` — важная информация
  - `WARNING` — только предупреждения
  - `ERROR` — только ошибки

### YOUTUBE_COOKIES_PATH
- **Что**: Путь к файлу cookies для YouTube
- **Обязательно**: ❌ НЕТ
- **Когда нужно**: Для видео 18+ или приватных
- **Как получить**:
  1. Установи расширение "Get cookies.txt LOCALLY"
  2. Зайди на youtube.com
  3. Экспортируй cookies
  4. Положи файл в `music-bot/cookies.txt`
  5. Раскомментируй строку в .env

---

## 🚀 Примеры конфигураций

### Для домашнего использования
```env
DISCORD_TOKEN=твой_токен
BOT_OWNER_ID=твой_id
USE_REDIS=false
DEFAULT_VOLUME=80
INACTIVITY_TIMEOUT=600
LOG_LEVEL=INFO
```

### Для маленького сервера (10-50 человек)
```env
DISCORD_TOKEN=твой_токен
BOT_OWNER_ID=твой_id
USE_REDIS=false
DEFAULT_VOLUME=100
MAX_QUEUE_SIZE=200
INACTIVITY_TIMEOUT=300
RATE_LIMIT_COMMANDS=15
LOG_LEVEL=INFO
```

### Для большого сервера (100+ человек)
```env
DISCORD_TOKEN=твой_токен
BOT_OWNER_ID=твой_id
USE_REDIS=true
REDIS_URL=redis://redis:6379/0
DEFAULT_VOLUME=100
MAX_QUEUE_SIZE=1000
INACTIVITY_TIMEOUT=600
RATE_LIMIT_COMMANDS=30
LOG_LEVEL=WARNING
```

### Для разработки/отладки
```env
DISCORD_TOKEN=твой_токен
BOT_OWNER_ID=твой_id
USE_REDIS=false
LOG_LEVEL=DEBUG
INACTIVITY_TIMEOUT=60
```

---

## 🔧 Как изменить настройки

1. **Открой файл .env**:
   ```bash
   nano .env
   # или
   notepad .env
   ```

2. **Измени нужные значения**

3. **Сохрани файл**

4. **Перезапусти бота**:
   ```bash
   docker compose down
   docker compose up --build
   ```

---

## ❓ Дополнительные вопросы

### Можно ли запустить без Docker?

Да, но нужно:
1. Установить Python 3.11+
2. Установить FFmpeg
3. Установить зависимости: `pip install -r requirements.txt`
4. Запустить: `python -m bot`

Docker проще, так как всё уже настроено.

---

### Как обновить бота?

```bash
# Остановить
docker compose down

# Обновить код (если есть изменения)
git pull

# Пересобрать и запустить
docker compose up --build
```

---

### Как запустить в фоне?

```bash
docker compose up -d
```

Посмотреть логи:
```bash
docker compose logs -f bot
```

Остановить:
```bash
docker compose down
```

---

### Сколько ресурсов нужно?

**Минимум**:
- CPU: 1 ядро
- RAM: 512 МБ
- Диск: 1 ГБ
- Интернет: 5 Мбит/с

**Рекомендуется**:
- CPU: 2 ядра
- RAM: 1 ГБ
- Диск: 2 ГБ
- Интернет: 10 Мбит/с

**Для больших серверов**:
- CPU: 4 ядра
- RAM: 2 ГБ
- Диск: 5 ГБ
- Интернет: 20+ Мбит/с

---

### Можно ли запустить на хостинге?

Да! Подходит любой VPS/VDS с Docker:
- DigitalOcean
- Linode
- Vultr
- Hetzner
- AWS EC2
- Google Cloud
- Azure

Или специализированные Discord bot хостинги:
- Heroku (с ограничениями)
- Railway
- Render

---

### Безопасно ли это?

✅ **Безопасно, если**:
- Не публикуешь `.env` файл
- Не делишься токеном
- Используешь официальный код

⚠️ **Риски**:
- Если токен утечёт — кто-то может управлять ботом
- Решение: сбрось токен в Developer Portal

---

### Законно ли это?

⚖️ **Юридически**:
- Бот для личного использования — ✅ OK
- Соблюдай Terms of Service YouTube/SoundCloud
- Не используй для коммерции без лицензий
- Не распространяй защищённый контент

---

## 📞 Нужна помощь?

1. Прочитай [Quick Start](QUICK_START.ru.md)
2. Посмотри [примеры](EXAMPLES.ru.md)
3. Проверь [полную документацию](README.ru.md)
4. Посмотри логи: `docker compose logs bot`

---

**Удачи! 🎵**
