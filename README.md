# 🎵 Discord Музыкальный Бот

Полнофункциональный музыкальный бот для Discord на Python с поддержкой YouTube и SoundCloud, управлением очередью, плейлистами и многим другим.

## ✨ Возможности

### Основные функции
- **Поддержка нескольких источников**: YouTube (URL + поиск), SoundCloud (URL + поиск)
- **Управление очередью**: Добавление, удаление, перемешивание, повтор треков
- **Управление воспроизведением**: Воспроизведение, пауза, продолжение, пропуск, перемотка, громкость
- **Пользовательские плейлисты**: Сохранение и загрузка личных плейлистов
- **Сохранение данных**: Очередь сохраняется при перезапуске бота (SQLite/Redis)
- **Slash-команды**: Современные команды Discord

### Дополнительные функции
- Поиск с предпросмотром (топ-5 результатов с выбором)
- Режимы повтора: Выкл, Один (повтор трека), Все (повтор очереди)
- Контроль громкости (0-200%)
- Поддержка DJ-роли для ограниченных команд
- Ограничение частоты команд (защита от спама)
- Корректное завершение работы и обработка ошибок
- Автоотключение при бездействии

## 🚀 Быстрый старт

### Что нужно
- Docker и Docker Compose
- Токен Discord бота ([Получить здесь](https://discord.com/developers/applications))

### Установка

**Шаг 1: Создай Discord бота**

1. Перейди на https://discord.com/developers/applications
2. Нажми **"New Application"** → придумай имя → Create
3. Перейди в раздел **"Bot"** слева
4. Нажми **"Reset Token"** → скопируй токен (он понадобится!)
5. Включи **"Message Content Intent"** (внизу страницы)
6. Сохрани изменения

**Шаг 2: Получи свой Discord ID**

1. В Discord: Настройки → Расширенные → включи **"Режим разработчика"**
2. Нажми правой кнопкой на своё имя → **"Копировать ID пользователя"**

**Шаг 3: Настрой бота**

```bash
cd music-bot
cp .env.example .env
```

Открой файл `.env` и заполни:
```env
DISCORD_TOKEN=твой_токен_бота_сюда
BOT_OWNER_ID=твой_discord_id_сюда
USE_REDIS=false
```

**Шаг 4: Запусти бота**

```bash
docker compose up --build
```

При первом запуске Docker скачает образы и соберёт контейнер (займёт несколько минут).

**Шаг 5: Пригласи бота на сервер**

1. В Discord Developer Portal перейди в **OAuth2 → URL Generator**
2. Выбери галочки:
   - **Scopes**: `bot`, `applications.commands`
   - **Bot Permissions**: 
     - Send Messages
     - Embed Links
     - Connect
     - Speak
     - Use Voice Activity
3. Скопируй ссылку внизу и открой в браузере
4. Выбери свой сервер и подтверди

**Готово!** Бот должен появиться на сервере. Попробуй `/ping` или `/help`

### С Redis (опционально, для продвинутых)

Redis нужен только если ты хочешь запустить несколько копий бота или нужна более быстрая база данных.

```bash
# В .env измени:
USE_REDIS=true

# Запусти с Redis:
docker compose --profile redis up --build
```

## 📝 Команды

### Музыкальные команды
| Команда | Описание |
|---------|----------|
| `/play <запрос>` | Воспроизвести песню по URL или поиску |
| `/search <запрос>` | Найти и выбрать из результатов |
| `/pause` | Поставить на паузу |
| `/resume` | Продолжить воспроизведение |
| `/skip` | Пропустить трек |
| `/stop` | Остановить и очистить очередь |
| `/seek <секунды>` | Перемотать на позицию |
| `/volume <0-200>` | Установить громкость |

### Команды очереди
| Команда | Описание |
|---------|----------|
| `/queue [страница]` | Показать очередь |
| `/now` | Показать текущий трек |
| `/remove <позиция>` | Удалить трек из очереди |
| `/clear` | Очистить очередь |
| `/shuffle` | Перемешать очередь |
| `/loop <off\|one\|all>` | Режим повтора |

### Команды плейлистов
| Команда | Описание |
|---------|----------|
| `/playlist save <имя>` | Сохранить очередь как плейлист |
| `/playlist load <имя>` | Загрузить плейлист |
| `/playlist list` | Список твоих плейлистов |
| `/playlist delete <имя>` | Удалить плейлист |

### Утилиты
| Команда | Описание |
|---------|----------|
| `/join` | Подключиться к голосовому каналу |
| `/leave` | Отключиться от канала |
| `/ping` | Проверить задержку |
| `/info` | Информация о боте |
| `/help` | Список команд |
| `/invite` | Получить ссылку-приглашение |

### Админ-команды (только для владельца)
| Команда | Описание |
|---------|----------|
| `/reload <cog>` | Перезагрузить модуль |
| `/sync` | Синхронизировать slash-команды |
| `/shutdown` | Выключить бота |
| `/servers` | Список серверов |

## 🏗️ Архитектура

### Структура проекта
```
music-bot/
├── bot/
│   ├── __main__.py          # Точка входа
│   ├── core/
│   │   ├── bot.py           # Главный класс бота
│   │   └── config.py        # Конфигурация
│   ├── cogs/
│   │   ├── music.py         # Музыкальные команды
│   │   ├── admin.py         # Админ-команды
│   │   └── utility.py       # Утилиты
│   ├── database/
│   │   └── manager.py       # Сохранение данных (Redis/SQLite)
│   ├── music/
│   │   ├── track.py         # Модель трека
│   │   ├── queue.py         # Управление очередью
│   │   ├── player.py        # Аудио-плеер
│   │   └── extractor.py     # Обёртка для yt-dlp
│   └── utils/
│       ├── checks.py        # Проверки прав
│       └── embeds.py        # Красивые сообщения
├── tests/                   # Тесты
├── Dockerfile               # Python 3.11 + FFmpeg
├── docker-compose.yml       # Бот + Redis
├── requirements.txt         # Зависимости
└── .env                     # Твоя конфигурация
```

### Как работает стриминг

1. **Ввод URL/Поиск**: Пользователь даёт YouTube/SoundCloud URL или поисковый запрос
2. **Извлечение**: `yt-dlp` извлекает метаданные и URL потока
3. **Стриминг**: `FFmpeg` стримит аудио напрямую в Discord (без скачивания файла)
4. **Воспроизведение**: `discord.py` управляет голосовым подключением

**Почему yt-dlp + FFmpeg?**
- `yt-dlp` — самый поддерживаемый экстрактор YouTube с широкой поддержкой платформ
- `FFmpeg` эффективно обрабатывает аудио и стриминг
- Не нужно скачивать файлы — стримит напрямую из источника
- Поддерживает перемотку, контроль громкости и переподключение

### База данных

Бот поддерживает два варианта хранения данных:

**SQLite (по умолчанию)**
- Нулевая настройка
- Данные хранятся в `data/music_bot.db`
- Подходит для одного экземпляра бота

**Redis (опционально)**
- Лучше для нескольких экземпляров бота
- Быстрее для ботов с большой нагрузкой
- Включается через `USE_REDIS=true`

## 🔧 Настройка

### Переменные окружения

| Переменная | Обязательна | По умолчанию | Описание |
|------------|-------------|--------------|----------|
| `DISCORD_TOKEN` | Да | - | Токен бота |
| `BOT_OWNER_ID` | Нет | - | Discord ID владельца |
| `COMMAND_PREFIX` | Нет | `!` | Префикс текстовых команд |
| `USE_REDIS` | Нет | `false` | Использовать Redis |
| `REDIS_URL` | Нет | `redis://redis:6379/0` | URL подключения к Redis |
| `DEFAULT_VOLUME` | Нет | `100` | Громкость по умолчанию (0-200) |
| `MAX_QUEUE_SIZE` | Нет | `500` | Макс. треков в очереди |
| `INACTIVITY_TIMEOUT` | Нет | `300` | Отключение через N секунд |
| `RATE_LIMIT_COMMANDS` | Нет | `20` | Макс. команд/минуту/пользователя |
| `LOG_LEVEL` | Нет | `INFO` | Уровень логирования |

## 🔒 Права и ограничения

### DJ-роль
Команды типа `/stop`, `/clear` требуют:
- Роль "DJ" (регистр не важен)
- Право "Управление сервером"
- Или быть одному в голосовом канале с ботом

### Ограничение частоты команд
- По умолчанию: 20 команд в минуту на пользователя
- Настраивается через `RATE_LIMIT_COMMANDS`
- Защищает от спама команд

## 🎛️ Интеграция с Lavalink (опционально)

Для ботов с большой нагрузкой или нескольких экземпляров рассмотри использование [Lavalink](https://github.com/lavalink-devs/Lavalink) вместо встроенного FFmpeg.

### Зачем Lavalink?
- Выносит обработку аудио в отдельный сервис
- Лучшая масштабируемость для больших ботов
- Поддержка балансировки нагрузки
- Меньше использования памяти на экземплярах бота

### Настройка с Lavalink

1. **Раскомментируй сервис Lavalink** в `docker-compose.yml`

2. **Создай конфиг Lavalink** в `lavalink/application.yml`:
   ```yaml
   server:
     port: 2333
     address: 0.0.0.0
   lavalink:
     server:
       password: "youshallnotpass"
       sources:
         youtube: true
         soundcloud: true
   ```

3. **Установи wavelink** (клиент Lavalink для discord.py):
   ```bash
   pip install wavelink
   ```

4. **Измени плеер** на использование wavelink вместо FFmpeg
   (См. документацию wavelink для деталей реализации)

5. **Запусти с профилем Lavalink**:
   ```bash
   docker compose --profile lavalink up --build
   ```

## 🧪 Тестирование

```bash
# Установи dev-зависимости
pip install -r requirements.txt

# Запусти тесты
pytest

# С покрытием кода
pytest --cov=bot
```

## 📋 Разработка

### Локальная разработка (без Docker)

1. **Создай виртуальное окружение**
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/Mac
   venv\Scripts\activate     # Windows
   ```

2. **Установи зависимости**
   ```bash
   pip install -r requirements.txt
   ```

3. **Установи FFmpeg**
   - Windows: `choco install ffmpeg` или скачай с ffmpeg.org
   - Mac: `brew install ffmpeg`
   - Linux: `apt install ffmpeg`

4. **Запусти бота**
   ```bash
   python -m bot
   ```

### Добавление новых команд

1. Создай или измени cog в `bot/cogs/`
2. Используй декоратор `@app_commands.command()`
3. Добавь проверки прав при необходимости
4. Перезагрузи через `/reload <имя_cog>`

## ⚠️ Юридическое уведомление

### Соблюдение условий использования

Этот бот предназначен для личного/образовательного использования. При использовании бота:

- **YouTube**: Соблюдай Условия использования YouTube. Этот бот стримит аудио для личного прослушивания, аналогично тому, как работает сайт YouTube. Коммерческое использование или распространение контента запрещено.

- **SoundCloud**: Соблюдай Условия использования SoundCloud и права артистов.

- **Авторские права**: Не используй этот бот для нарушения авторских прав. Операторы бота несут ответственность за соблюдение местных законов.

### Примечание о Spotify

Spotify не предоставляет прямой доступ к API для стриминга аудио. Для воспроизведения треков Spotify:
1. Бот ищет трек на YouTube
2. Воспроизводит версию с YouTube

Это распространённый обход, используемый большинством музыкальных ботов.

## 🐛 Решение проблем

### Бот не отвечает на команды
- Убедись, что slash-команды синхронизированы: используй `/sync`
- Проверь, что у бота есть нужные права на сервере
- Убедись, что бот онлайн в Discord

### Аудио не воспроизводится
- Убедись, что FFmpeg установлен в контейнере
- Проверь, что у бота есть права Connect и Speak
- Убедись, что голосовой канал не переполнен

### Ошибки "Результаты не найдены"
- YouTube может ограничивать запросы. Попробуй позже
- Некоторые видео заблокированы по региону или возрасту
- Для контента 18+ предоставь файл cookies

### Высокая задержка/заикание
- Проверь сетевое подключение сервера
- Рассмотри использование Lavalink для лучшей производительности
- Уменьши `DEFAULT_VOLUME`, если высокая загрузка CPU

### Как остановить бота
```bash
# В терминале где запущен docker compose:
Ctrl+C

# Или в другом терминале:
docker compose down
```

### Как посмотреть логи
```bash
docker compose logs -f bot
```

### Как обновить бота
```bash
docker compose down
docker compose up --build
```

## 📊 Мониторинг

### Проверка статуса
```bash
# Список запущенных контейнеров
docker compose ps

# Логи бота
docker compose logs bot

# Логи Redis (если используется)
docker compose logs redis
```

### Использование ресурсов
```bash
# Статистика контейнеров
docker stats
```

## 🔐 Безопасность

### Важно!
- **Никогда не публикуй** файл `.env` или токен бота
- Добавь `.env` в `.gitignore` (уже добавлено)
- Если токен утёк — сразу сбрось его в Discord Developer Portal
- Не давай права администратора боту без необходимости

### Рекомендуемые права бота
Минимальные права для работы:
- Send Messages (Отправка сообщений)
- Embed Links (Встраивание ссылок)
- Connect (Подключение к голосовым каналам)
- Speak (Воспроизведение звука)
- Use Voice Activity (Использование голосовой активности)

## 💡 Советы

### Для лучшей производительности
- Используй VPS с хорошим интернетом (минимум 10 Мбит/с)
- Рассмотри Lavalink для серверов с 100+ пользователями
- Включи Redis для нескольких экземпляров бота

### Для стабильности
- Настрой автоперезапуск: `restart: unless-stopped` в docker-compose (уже настроено)
- Мониторь логи на ошибки
- Регулярно обновляй зависимости

### Для пользователей
- Создай роль "DJ" для доверенных пользователей
- Настрой `MAX_QUEUE_SIZE` под свои нужды
- Используй `/help` для обучения пользователей

## 📄 Лицензия

MIT License — свободно используй и модифицируй для своих проектов.

## 🤝 Вклад в проект

Вклад приветствуется! Пожалуйста:
1. Сделай fork репозитория
2. Создай ветку для новой функции
3. Отправь pull request

## 📞 Поддержка

Если возникли проблемы:
1. Проверь раздел "Решение проблем" выше
2. Посмотри логи: `docker compose logs bot`
3. Убедись, что все переменные в `.env` заполнены правильно

---

Сделано с ❤️ и Python

**Приятного использования! 🎵**
